RewriteEngine On
RewriteBase /

# Let existing files and directories resolve normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send app routes back to the SPA entry point.
RewriteRule ^ index.html [L]
